const initialState = {
  globalError: ""
};

const errorsReducer = (state = initialState, action: any) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default errorsReducer;